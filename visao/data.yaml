# Ground Path
# Caminho base (podes usar o caminho completo se preferires)
# Root folder of your dataset. Update this path or create the example structure:
#   ./data_sample/
#       images/
path: ./data_sample

# Relative folder to the 'path' above
train: images
val: images

# class definition
names:
  0: person